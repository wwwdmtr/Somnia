generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Dream {
    id          String  @id @default(uuid())
    nickname    String?
    title       String
    description String
    text        String
}
